# Python environments (regenerated: CondaPkg.toml pins triton; venv is legacy)
venv/
.CondaPkg/

# scratch
*.tmp

# machine-specific (e.g. CUDA_Runtime_jll version pin for the cluster)
LocalPreferences.toml

# serialized benchmark matrices (see bench/spmm_flow.jl header)
bench/data/
